services:
  bot:
    container_name: layer3-telegram-bot
    image: layer3-telegram-bot:local
    build: .
    restart: unless-stopped
    user: root
    env_file: .env
    volumes:
      - ./data:/app/data
    shm_size: 1gb
